package prefixSum;
import java.util.*;
import java.io.*;
public class B25682 {
	static int N, M, K;
	static char[][] firstBlack;
	static char[][] firstWhite;
	static int[][] firstBlackGraph;
	static int[][] firstWhiteGraph;
	static void reset() {
		boolean check = false;
		firstBlack = new char[N][M];
		firstWhite = new char[N][M];
		for (int i = 0; i < N; i++) {
			for (int j = 0; j < M; j++) {
				check = !check;
				if (check) {
					firstBlack[i][j] = 'B';
					firstWhite[i][j] = 'W';
				} else {
					firstBlack[i][j] = 'W';
					firstWhite[i][j] = 'B';
				}
			}
			check = !check;
		}
	}
	public static void main(String[] args) throws IOException {
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		StringTokenizer st = new StringTokenizer(br.readLine());
		N = Integer.parseInt(st.nextToken());
		M = Integer.parseInt(st.nextToken());
		K = Integer.parseInt(st.nextToken());
		reset();
		for (int i = 0; i < N; i++) {
			char[] input = br.readLine().toCharArray();
			for (int j = 0; j < M; j++) {
				
			}
		}
		
	}
}
