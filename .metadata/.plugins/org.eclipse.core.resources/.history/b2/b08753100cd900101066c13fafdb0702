package dp;
import java.io.*;
import java.util.*;

// 결국 전기줄이 서로 겹치지 않기 위해서는 전깃줄이 오름차순 형식으로 연결되거나
// 혹은 내림차순 형식으로 연결되어야 함
// 하지만 같은 위치를 가리키는 전깃줄이 있다면 그걸 기점으로는 내림차순, 오름차순이 변경될 수 있음
// 결국 구간별로 연산을 진행하면 되지 않을까 싶음
// 하지만 끝내 어느 구간에나 최적인 값이 존재하기에 모든 위치에 대해 dp를 저장해야 할듯
// 보내는 쪽? 아니면 받는 쪽? 어느곳에서 그걸 정리해야 할까?
// 일단 보내는 쪽 부터 시작해보자
public class B2565 {
	static int N;
	// 보내는쪽이 증가할때, 받는쪽이 감소한다면 문제 발생
	// (a0, b0) - (a1, b1)
	// a0 < a1 인데 b0 > b1 라면 문제 발생
	public static void main(String[] args) throws IOException {
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		N = Integer.parseInt(br.readLine());
		Queue<int[]> q = new ArrayDeque<>();
		for (int i = 0; i < N; i++) {
			StringTokenizer st = new StringTokenizer(br.readLine());
			int a = Integer.parseInt(st.nextToken());
			int b = Integer.parseInt(st.nextToken());
		}
	}
}
