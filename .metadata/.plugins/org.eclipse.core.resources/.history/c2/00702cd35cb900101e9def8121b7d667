package bfs;
import java.util.*;
import java.io.*;

public class B16953 {
	public static void main(String[] args) throws IOException {
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		StringTokenizer st = new StringTokenizer(br.readLine());
		int A = Integer.parseInt(st.nextToken());
		int B = Integer.parseInt(st.nextToken());
		Deque<Integer> deque = new ArrayDeque<>();
		int[] graph = new int[1000000001];
		graph[A] = 1;
		deque.offerLast(A);
		while (!deque.isEmpty()) {
			int num = deque.pollFirst();
			if (num == B) break;
			int dx1 = num * 2;
			if (dx1 < graph.length && graph[dx1] == 0) {
				graph[dx1] = graph[num] + 1;
				deque.offerLast(dx1);
			}
			long dx2 = num * 10 + 1;
			if (graph.length < dx2) continue;
			int dx3 = (int)dx2;
			if (graph[dx3] == 0) {
				graph[dx3] = graph[num] + 1;
				deque.offerLast(dx3);
			}
		}
		if (graph[B] == 0) {
			System.out.println(-1);
		} else {
			System.out.println(graph[B]);
		}
	}
}
